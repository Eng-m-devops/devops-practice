# Inventory file for Enterprise DevOps Architecture deployment

[webservers]
# Replace with target IP address or hostname
app-node-01 ansible_host=127.0.0.1 ansible_connection=local

[dbservers]
# Add separate database servers if needed
# db-node-01 ansible_host=10.0.1.20

[all:vars]
ansible_python_interpreter=/usr/bin/python3
